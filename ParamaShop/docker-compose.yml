# version: '3.8'
# services:
#   db:
#     image: postgres:15
#     restart: always
#     environment:
#       POSTGRES_DB: parama_shopping_db
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: MohanSQL
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data

# volumes:
#   pgdata:

version: '3.9'

services:
  postgres:
    image: postgres:14-alpine
    ports:
      - 5432:5432
    volumes:
      - ~/apps/postgres:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=S3cret
      - POSTGRES_USER=citizix_user
      - POSTGRES_DB=citizix_db